---
import { MapPinIcon } from "@lucide/astro";
interface Props {
  location: string;
}
const { location } = Astro.props;
---

<span class="flex flex-row items-center gap-2 text-gray-500 text-lg"
  ><MapPinIcon size={16} /> {location}</span
>
